# SOCKET IO EXAMPLE
**Install package**
	`npm i`
  
**Start server**
	`node server.js`

**Start client**
	`node client.js`	

**For example:**

	Send request to `localhost:3000/push` with any body data.
  
